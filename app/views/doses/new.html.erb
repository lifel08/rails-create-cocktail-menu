<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>Add incredients to @cocktails.name</h1>
      <%= simple_form_for @dose do |f| %>
        <%= f.input :amount, as: :numeric %>
<!--         <%= f.input :ingredients, collection: INGREDIENT::NAME %>
 -->        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
